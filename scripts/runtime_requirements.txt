# Managed local runtime requirements for Liberty.
# Keep these versions conservative so Python 3.9 remains supported on macOS and Windows.
numpy<2
modelscope>=1.24,<2
huggingface_hub>=0.30,<1
funasr>=1.1,<2
